apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: eks-observability
  labels:
    app: backend-api
spec:
  type: ClusterIP
  selector:
    app: backend-api
  ports:
    - name: http
      port: 80
      targetPort: 4000